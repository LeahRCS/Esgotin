# Copie para `.env.server` e ajuste credenciais.
# PostgreSQL em desenvolvimento (ex.: `wasp db start` ou instância local).
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/esgotin"
ADMIN_PASSWORD="minha_senha_super_secreta_root"
